{
  "name": "pethealthapp_webapi",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node dist/app.js",
    "build": "rm -rf dist/* && find src -name '*.ts' | xargs npx esbuild --format=esm --platform=node --outdir=dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.602.0",
    "@types/express": "^4.17.21",
    "esbuild": "^0.23.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
