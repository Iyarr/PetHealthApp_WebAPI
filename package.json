{
  "name": "pethealthapp_webapi",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node dist/app.js",
    "build": "find src -name '*.ts' | xargs npx esbuild --format=esm --platform=node --outdir=dist",
    "test": "rm -rf dist && npm run build && node --test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "esbuild": "^0.23.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.602.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
